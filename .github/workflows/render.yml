name: Render video

# The render farm. Dispatched by the app; each run renders exactly one job.
# Long timelines are split into independent chunks so completed work can be
# resumed and stitched without restarting the entire render.
on:
  workflow_dispatch:
    inputs:
      job_id:
        description: Render job id
        required: true
      job_token:
        description: Single-job access token
        required: true
      app_url:
        description: Base URL of the AutoCut Studio app
        required: true
      chunk_count:
        description: Number of parallel render chunks
        required: true

jobs:
  prepare:
    runs-on: ubuntu-24.04
    outputs:
      chunks: ${{ steps.set.outputs.chunks }}
    steps:
      - id: set
        run: |
          CHUNKS=$(node -e "console.log(JSON.stringify(Array.from({length: ${{ inputs.chunk_count }}}, (_, i) => i)))")
          echo "chunks=$CHUNKS" >> "$GITHUB_OUTPUT"

  render:
    needs: prepare
    runs-on: ubuntu-24.04
    # GitHub-hosted jobs can run for up to 6 hours. Keep a slow chunk alive
    # for up to 5 hours while completed chunks remain checkpointed independently.
    timeout-minutes: 300
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix:
        chunk_index: ${{ fromJson(needs.prepare.outputs.chunks) }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: render-worker/package-lock.json

      - name: Install Chromium dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 \
            libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 \
            libgbm1 libpango-1.0-0 libcairo2 libasound2t64 ffmpeg

      - name: Install worker dependencies
        working-directory: render-worker
        run: npm ci --no-audit --no-fund

      - name: Install app dependencies (for composition module resolution)
        run: npm install --no-audit --no-fund

      - name: Cache Chrome Headless Shell
        uses: actions/cache@v4
        with:
          path: render-worker/node_modules/.remotion
          key: chrome-headless-shell-${{ hashFiles('render-worker/package-lock.json', 'render-worker/package.json') }}

      - name: Render chunk
        working-directory: render-worker
        env:
          JOB_ID: ${{ inputs.job_id }}
          JOB_TOKEN: ${{ inputs.job_token }}
          APP_URL: ${{ inputs.app_url }}
          CHUNK_INDEX: ${{ matrix.chunk_index }}
          CHUNK_COUNT: ${{ inputs.chunk_count }}
          MAX_CHUNK_ATTEMPTS: 3
          # Keep two render processes on the 4-vCPU GitHub runner. Each process
          # also uses browser/media work, so four processes previously caused
          # CPU oversubscription and long stalls/crashes.
          RENDER_CONCURRENCY: 2
        run: |
          for attempt in 1 2 3; do
            echo "Chunk attempt $attempt/3"
            node render.mjs && exit 0
            if [ "$attempt" -lt 3 ]; then
              echo "Chunk failed; retrying only this chunk in 15 seconds"
              sleep 15
            fi
          done
          exit 1

  stitch:
    needs: render
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: render-worker/package-lock.json

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ffmpeg

      - name: Stitch chunks
        working-directory: render-worker
        env:
          JOB_ID: ${{ inputs.job_id }}
          JOB_TOKEN: ${{ inputs.job_token }}
          APP_URL: ${{ inputs.app_url }}
          CHUNK_COUNT: ${{ inputs.chunk_count }}
        run: node stitch.mjs
